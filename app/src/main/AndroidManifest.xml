<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kopfc.privatelessons" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".controller.PrivateLessonsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.ScheduleActivity"
            android:label="@string/title_activity_schedule" >
        </activity>
        <activity
            android:name=".controller.BiosActivity"
            android:label="@string/title_activity_bios" >
        </activity>
        <activity
            android:name=".controller.DrillsActivity"
            android:label="@string/title_activity_drills" >
        </activity>
        <activity
            android:name=".controller.ClassesActivity"
            android:label="@string/title_activity_classes" >
        </activity>
    </application>

</manifest>
